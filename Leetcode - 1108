class Solution {
public:
    string defangIPaddr(string address) {
        string word;
        for (int i = 0 ; i < address.size() ; i++){
            if (address[i] == '.'){
                word += "[.]";
            }
            else
                word += address[i];
        }
         return word;
    }
};
