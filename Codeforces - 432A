#include <bits/stdc++.h>

using namespace std;

int main()
{
    int count = 0 , n , k , y;
    scanf("%d%d",&n,&k);
    for (int i = 0 ; i < n ; i++){
        scanf("%d",&y);
        if (5 - y >= k){
            count++;
        }
    }
    printf("%d",count/3);
    return 0;
}
